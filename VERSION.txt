Reply v1.3.2 — фикс белого экрана + спиннер + светлая тема читаема
- Фикс «стоит на загрузке, только экран без текстов и кнопок»: 
  - index.html теперь имеет встроенный splash со спиннером (крутящийся круг) — даже если JS не загрузился, видно загрузку, без кнопок Сбросить (по просьбе убраны).
  - main.js упрощён до простого init, без сложных fallback с кнопками сброса, с авто-ретрай через 1с.
  - renderSplash() теперь всегда с ready классом, спиннером в центре, try/catch вокруг темы и звука, клик по экрану тоже ведёт дальше.
  - css: спиннер .spinner 44px border-top #ff5e7e animation spin, light theme border-color адаптирован.
- Светлая тема чат: раньше очень темно, ничего не видно — теперь chat-sheet светлый, пузыри партнёра белые, таймер читаемый, actions/input светлые.
- Бегунок в меню еды: .menu-list max-height 52vh overflow-y auto + thin scrollbar 6px.
- Адаптив: mobile 100%, tablet 560px, desktop >=900px 1100px grid, date-screen 1fr 420px, loc-grid 3 колонки, coll-grid 4-5.
- Возраст: 18-60 (было 18-35 мало), слайдеры max 60.
- Тема в самом начале: splash + onboarding step 0 🌙/☀️/⚙️, честный онбординг без предвыбора, время утро-ночь 08:00-00:30 + Сразу в чат, лимит лайков 5/день отказы безлимит.
