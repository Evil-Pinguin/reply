# Reply · ключ Groq API (внешний ИИ-собеседник)
# ─────────────────────────────────────────────────────────────────────────────
# Скопируйте этот файл в «groq_key.txt» и вставьте свой ключ вместо XXX.
# groq_key.txt добавлен в .gitignore — ключ НИКОГДА не попадёт в репозиторий.
#
#   GROQ_API_KEY=sk-XXX
#
# Альтернатива: задайте переменную окружения перед запуском сервера:
#
#   export GROQ_API_KEY=sk-XXX
#   python3 server.py
#
# Ключ получают на https://console.groq.com/keys (бесплатный тариф есть).
#
# Без ключа приложение работает в автономном режиме: вместо внешнего ИИ
# отвечает встроенный генеративный «мозг» (локальные фразы и анализ тем).
